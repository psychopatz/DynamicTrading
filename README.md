# DynamicTrading

Dynamic Trading now keeps quest and objective gameplay in a separate addon mod named `Dynamic Objectives`.

The base `DynamicTrading` and `DynamicTradingV2` mods remain playable without the addon enabled.
